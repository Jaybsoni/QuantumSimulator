# Setup for Quantum Simulator
from setuptools import setup

setup(name='qsim',
      version='0.1',
      description='A quantum simulator',
      author='Jay Soni',
      packages=['qsim'],
      license='MIT'
      install_requires=[
          'numpy',
          'matplotlib'])
